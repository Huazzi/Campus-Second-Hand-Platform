# 发布商品功能重构部署指南

## 问题解决

### 1. 修复的主要问题
- **500错误修复**：前端请求方式从GET改为POST
- **参数映射优化**：重新设计了前后端参数映射关系
- **数据库结构优化**：扩展了goods表以支持通用商品

### 2. 核心修改内容

#### 前端修改 (frontend/pages/add_good/)
- `add_good.js`: 修复请求方式，优化参数映射
- `add_good.wxml`: 添加新的表单字段（品牌、型号、可议价等）
- `add_good.wxss`: 优化样式，添加新字段样式

#### 后端修改 (base-api/)
- `AddGoodServlet.java`: 支持新的参数结构，增强错误处理
- `Good.java`: 扩展实体类，添加新字段
- `GoodDaoImpl.java`: 更新数据库操作以支持新字段

#### 数据库修改
- `goods_table_redesign.sql`: 数据库表结构升级脚本

## 部署步骤

### 1. 数据库升级
```sql
-- 执行数据库升级脚本
source goods_table_redesign.sql;
```

**重要提示**：
- 在生产环境执行前，请先备份数据库
- 脚本包含数据迁移逻辑，会保留现有数据
- 新增字段都设置为可空，不会影响现有数据

### 2. 后端部署
1. 重新编译Java项目
2. 部署更新后的war包
3. 重启应用服务器

### 3. 前端部署
1. 更新小程序代码
2. 重新上传到微信开发者工具
3. 提交审核（如需发布到生产环境）

## 测试指南

### 1. 基础功能测试

#### 测试用例1：图书类商品发布
- 填写商品名称：《Java编程思想》
- 选择分类：图书音像
- 填写ISBN：9787111213826（可选）
- 填写描述：经典Java学习书籍
- 设置价格：50元，原价：89元
- 选择成色：轻微使用痕迹
- 选择功能状态：功能完好
- 上传图片：1-3张
- 点击发布

**预期结果**：发布成功，返回商品ID

#### 测试用例2：电子产品发布
- 填写商品名称：iPhone 13
- 选择分类：数码产品
- 填写品牌：Apple
- 填写型号：iPhone 13 128GB
- 填写描述：9成新，无磕碰
- 设置价格：4500元，原价：5999元
- 选择成色：轻微使用痕迹
- 选择功能状态：功能完好
- 设置可议价：开启
- 上传图片：3-5张
- 点击发布

**预期结果**：发布成功，新字段正确保存

#### 测试用例3：生活用品发布
- 填写商品名称：宿舍台灯
- 选择分类：居家日用
- 填写描述：护眼台灯，调光调色
- 设置价格：80元，原价：150元
- 选择成色：明显使用痕迹
- 选择功能状态：有小问题但不影响使用
- 选择交易方式：当面交易
- 上传图片：2-3张
- 点击发布

**预期结果**：发布成功，所有字段正确保存

### 2. 错误处理测试

#### 测试用例4：必填字段验证
- 不填写商品名称，直接点击发布
- **预期结果**：显示"请输入商品名称"提示

#### 测试用例5：分类选择验证
- 不选择商品分类，直接点击发布
- **预期结果**：显示"请选择商品分类"提示

#### 测试用例6：图片上传验证
- 不上传图片，直接点击发布
- **预期结果**：显示"请选择商品图片"提示

### 3. 兼容性测试

#### 测试用例7：旧数据兼容性
- 查看现有商品是否正常显示
- 编辑现有商品是否正常
- **预期结果**：现有功能不受影响

### 4. 性能测试

#### 测试用例8：图片上传性能
- 上传9张图片（最大限制）
- 观察上传进度和成功率
- **预期结果**：所有图片成功上传，有进度提示

## 监控和日志

### 1. 关键日志位置
- 后端日志：查看AddGoodServlet的日志输出
- 数据库日志：监控goods表的插入操作
- 前端日志：微信开发者工具控制台

### 2. 监控指标
- 发布成功率
- 图片上传成功率
- 平均发布时间
- 错误率统计

## 回滚方案

如果出现严重问题，可以按以下步骤回滚：

### 1. 代码回滚
- 恢复到之前的代码版本
- 重新部署应用

### 2. 数据库回滚
```sql
-- 如果需要回滚数据库结构（谨慎操作）
-- 删除新增字段
ALTER TABLE goods DROP COLUMN original_price;
ALTER TABLE goods DROP COLUMN condition_status;
-- ... 其他新增字段
```

**注意**：数据库回滚会丢失新字段的数据，请谨慎操作。

## 常见问题解决

### 1. 500错误
- 检查请求方式是否为POST
- 检查参数名称是否正确
- 查看后端日志确定具体错误

### 2. 图片上传失败
- 检查图片存储目录权限
- 确认图片大小和格式
- 查看网络连接状态

### 3. 数据保存不完整
- 检查数据库字段长度限制
- 确认所有新字段都已添加
- 验证数据类型匹配

## 后续优化建议

1. **性能优化**：考虑图片压缩和CDN存储
2. **用户体验**：添加草稿保存功能
3. **数据分析**：添加商品发布统计
4. **安全加固**：增强输入验证和防护
